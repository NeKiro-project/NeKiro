{
  "name": "nek-agent-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm --filter ./apps/console run build",
    "typecheck": "pnpm --filter ./apps/console run typecheck",
    "test": "pnpm --filter ./apps/console run test",
    "test:contracts": "go test ./contracts",
    "check": "go test ./... && go vet ./... && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
