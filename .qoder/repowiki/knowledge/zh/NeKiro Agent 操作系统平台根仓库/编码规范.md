- 跨进程边界必须使用 `contracts/` 中已版本化的 JSON Schema / OpenAPI / A2A Profile，禁止在 Go 与 TS 之间共享内部实现类型。
- 新功能或架构变更必须先产出 `specs/<feature>/` 工件并按 observe→constitution→specify→clarify→plan→tasks→analyze→implement→tests→review→converge 流程推进，不得先写代码后补 spec。
- Control Plane 与 A2A Router 保持独立部署单元，Router 不得反向导入 Control Plane 内部模块，Agent-to-Agent 调用必须再次经过 Router 并传播父子 Invocation 标识。
- 所有失败路径（超时、取消、路由失败、协议失败、业务失败）必须在 Ledger 中以不同错误码区分记录，禁止用空值或默认 fallback 掩盖依赖故障。
- Commit 身份统一为 `Nene7ko_ <1604009816@qq.com>`，且仓库内禁止出现 API Key、Token 等密钥字段的明文或默认值。